// Re-export types from data/works.ts for backwards compatibility
export type { CategoryType, WorkItem } from './data/works';
export { WORKS, CATEGORIES, SITE_INFO, getWorkBySlug, getWorksByCategory } from './data/works';
